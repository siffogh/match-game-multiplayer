{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client:build": "cd client && npm run build",
    "server:build": "NODE_ENV=prod node server.js",
    "postinstall": "cd client && npm i",
    "server:dev": "node server.js",
    "client:dev": "cd client && npm run start",
    "dev": "run-p *:dev ",
    "build": "run-s *:build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hapi": "^17.5.4",
    "hapi-swagger": "^9.1.1",
    "inert": "^5.1.0",
    "socket.io": "^2.1.1",
    "vision": "^5.4.0"
  },
  "devDependencies": {
    "eslint": "^5.5.0",
    "eslint-config-prettier": "^3.0.1",
    "eslint-plugin-node": "^7.0.1",
    "eslint-plugin-prettier": "^2.6.2",
    "npm-run-all": "^4.1.3",
    "prettier": "^1.14.2"
  }
}
